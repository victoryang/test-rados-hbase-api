sudo: required
dist: trusty

language: go
go: 
  - 1.9.x

script: go build -o helloworld

deploy:
  provider: releases
  api_key: ${GH_TOKEN}
  file: helloworld
  skip_cleanup: true
  on:
    tags: true
